{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to host process",
            "type": "lldb",
            "request": "attach",
            "pid": "${command:pickMyProcess}"
        }
    ]
}
