name: Unit Tests

on:
  pull_request:
    types: [ opened, synchronize ]
    paths-ignore:
      - "README.md"
      - "**/*.md"
      - "scripts/*.sh"
      - "scripts/*.ps1"

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

jobs:
  run_tests:
    name: ${{ matrix.job_name }}
    strategy:
      fail-fast: true
      matrix:
        include:
          - job_name: Run Zig and Swift tests
            runner: macos-26
            suite: apple
            timeout_minutes: 10
          - job_name: Run Kotlin tests
            runner: ubuntu-latest
            suite: kotlin
            timeout_minutes: 5
    runs-on: ${{ matrix.runner }}
    timeout-minutes: ${{ matrix.timeout_minutes }}
    steps:
      - uses: actions/checkout@v6
      - uses: partout-io/action-prepare-xcode-build@master
        if: matrix.suite == 'apple'
        with:
          access-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Zig
        uses: mlugg/setup-zig@v2
        if: matrix.suite == 'apple'
      - name: Run Zig tests
        if: matrix.suite == 'apple'
        run: ci/run-tests.sh
      - name: Build active XCFramework slice
        if: matrix.suite == 'apple'
        run: scripts/build-xcframework.sh
      - name: Run Swift tests
        if: matrix.suite == 'apple'
        run: ci/run-swift-tests.sh
      - uses: actions/setup-java@v5
        if: matrix.suite == 'kotlin'
        with:
          distribution: temurin
          java-version: 21
      - name: Run Kotlin tests
        if: matrix.suite == 'kotlin'
        run: ci/run-kotlin-tests.sh
