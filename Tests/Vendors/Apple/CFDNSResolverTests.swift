// SPDX-FileCopyrightText: 2025 Davide De Rosa
//
// SPDX-License-Identifier: GPL-3.0

import _PartoutVendorsApple
import Foundation
import XCTest

final class CFDNSResolverTests: XCTestCase {
//    func test_givenResolver_whenResolveBeforeTimeout_thenReturnsResolvedRecords() async throws {
//        let hostname = "fixed.it"
//        let records = [DNSRecord(address: "5.9.152.28", isIPv6: false)]
//        let sut = SimpleDNSResolver {
//            CFDNSStrategy(hostname: $0)
//        }
//        let result = try await sut.resolve(hostname, timeout: 1000)
//        XCTAssertEqual(result, records)
//    }
//
//    func test_givenResolver_whenResolveAfterTimeout_thenFailsWithTimeout() async throws {
//        let hostname = "fixed.it"
//        let sut = SimpleDNSResolver {
//            CFDNSStrategy(hostname: $0)
//        }
//        do {
//            _ = try await sut.resolve(hostname, timeout: 1)
//            XCTFail(".resolve must fail")
//        } catch let error as PartoutError {
//            XCTAssertEqual(error.code, .dnsFailure)
//        } catch {
//            XCTFail("Unexpected error: \(error)")
//        }
//    }
}
